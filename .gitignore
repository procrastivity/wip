# Private, uncommitted project instructions
/CLAUDE.local.md

# wip's own rendered snapshot (wip refresh). Ignored by decision, not by
# one machine's git config (C6.8): .wip/ is per-clone working state, and
# wip doctor itself refuses to render into a tracked .wip/.
/.wip/
/.amp/portals/

# Build artifacts
/bin/
/dist/

# Nix
/result
/result-*
.direnv/

.DS_Store
