# w4 — Orchestration substrate (Solo/Duo) & playbook repair

Read `.wip/distillation/BRIEF.md` first. You are agent **w4**.

## Your deliverable

1. **The repaired, portable actor playbook set**: orchestrator / coordinator /
   researcher / builder. Fix the broken self-references in `workflow-portable-stub`
   (playbooks say `notes/playbook/...` but files live at `playbook/`; README checklist
   references `notes/playbook/README.md`). Recommend the corrected, `.wip`-aware layout
   (do these playbooks live under `.wip/` now? in a `playbook/` feature dir? decide and
   justify). Coordinate file locations with w1's `.wip` layout.
2. **When to use sub-agents at all** — and when NOT to. Give a decision rubric:
   single-agent vs orchestrator+coordinator+builders, tied to task size/parallelism/risk.
3. **Model / harness / provider selection guidance**: the tiered-spawn idea
   (`small`/`medium`/`large` → `agent_tool_id`) from `agent-tool-selection.md`; how to
   choose Claude vs Codex, Opus vs smaller; for Solo specifically the harness+provider
   axis. Make it a usable rubric, not prose.
4. **Solo todos / scratchpads / timers** as the durable coordination surface, and how
   they relate to `.wip/` files: what's the source of truth for "what's next" — Solo
   todos, `.wip/roadmap.md`, or both (and how they stay in sync)? This is central to the
   human's "deterministic state of things" goal — be opinionated.
5. **Idle-timer / escalation patterns** distilled into reusable guidance (the
   coordinator wake-up routing, retry/escalation policy).

## Read
`workflow-portable-stub/playbook/*.md` (ALL: shared-static, agent-tool-selection,
orchestrator, coordinator, researcher, builder) + `workflow-portable-stub/README.md`.
Call Solo MCP `help` and `help(topic="coordination")`, `help(topic="timers")` to ground
the substrate facts. Note Duo's tiered spawn if relevant.

## Output
`.wip/distillation/findings/w4-orchestration.md` per the BRIEF output contract.
Coordinate playbook file locations with w1; your "what's next" stance feeds w5's
`wip status`/`wip next`.
