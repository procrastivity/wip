# w1 — Planning model, vocabulary & `.wip` lifecycle

Read `.wip/distillation/BRIEF.md` first. You are agent **w1**.

## Your deliverable

The **canonical conceptual spine** the whole system hangs off of. Specifically:

1. **A finalized glossary of primitives** (`GLOSSARY.md`-ready). Resolve every naming
   tension, especially **Step vs Phase** — pick one, justify it. Define Actors,
   Collections, lifecycle states, and the verbs that move an artifact between states.
2. **A lifecycle state machine**: idea → proposal → brief → roadmap(round/step) →
   workplan(chunks) → execution → graduate-to-LDS | archive | backlog. Show the
   transitions, who/what triggers each, and what file moves where.
3. **The reconciliation of the three rival homes** (stub `notes/`, bizapps `.wip/`,
   symfony `playbook/notes/`) into the single `.wip/` initiative-scoped layout. Define
   the migration mapping (old path → new path) for each.
4. **The ideate→spec fork**: when does `spec-generator` (structured interview) apply vs
   an ad-hoc handoff doc → playbook? Define both entry paths into an initiative.
5. **`wip.yaml` manifest schema** (initiative list, per-feature flags, gitignore policy,
   LDS location) — enough that w3 (detection) and w5 (CLI) can build against it.

## Read
`workflow-portable-stub/` (playbook/ AND notes/, including project-planning-workflow-notes.md
and the proposals/roadmap READMEs), `bizapps-symfony-bot/.wip/` (README + COMMON + phases of
BOTH initiatives), `playbook/notes/` (the symfony project — roadmap/workplans/audit shapes),
and the `spec-generator` skill description.

## Output
`.wip/distillation/findings/w1-planning-model.md` per the BRIEF output contract.
Other workstreams DEPEND on your vocabulary — make it crisp and stable.
