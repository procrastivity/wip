# w3 — LDS discoverability, composability & the extract pipeline

Read `.wip/distillation/BRIEF.md` first. You are agent **w3**.

## Your deliverable

1. **Deterministic LDS detection**: solve the "is LDS installed, and WHERE (engineering/
   vs docs/ vs custom)?" problem. Design the contract — e.g. a top-level pointer or a
   `find -name .lds-manifest.yaml` convention plus what `wip doctor` / `wip detect`
   reports. Account for old-style (`hypomnema/docs/`) vs new-style
   (`playbook/engineering/`, `xcind/engineering/`).
2. **Composability**: how to run **LDS without Diátaxis** (and vice versa), and how the
   two coexist when both present (xcind keeps Diátaxis in `docs/`, LDS in `engineering/`).
   Define the feature flags this implies in `wip.yaml` (coordinate with w1).
3. **The extract/decompose pipeline**: how in-flight `.wip/` planning graduates into
   durable LDS (Specs/Decisions). Map this onto the EXISTING LDS verbs
   (`extract.md`, `create.md`, `migrate-to-two-track.md`, `maintenance/{sync,update,refine,audit}.md`)
   — do NOT invent a parallel system; wire `.wip` → those verbs. Note where bizapps
   already cross-references LDS (`.wip/.../COMMON.md` §11 → ADRs/specs).
4. **The detection contract for ALL features**, generalized: a small spec for how any
   composable feature (LDS, Diátaxis, changelog, prtend, Solo) advertises its presence
   so `wip doctor` is deterministic. This is the shared "is X installed/active" answer.

## Read
`layered-documentation-system/` (read `LAYERED-DOCUMENTATION-SYSTEM.md`, `install.md`,
`extract.md`, `create.md`, `migrate-to-two-track.md`, `analyze.md`, `maintenance/*.md`,
`schemas/extraction-manifest.schema.yaml`), both `.lds-manifest.yaml` files,
`xcind/engineering/README.md` + `xcind/docs/README.md` (LDS+Diátaxis coexistence),
`hypomnema/docs/` layout, `bizapps-symfony-bot/.wip/conversations/COMMON.md` §11.

## Output
`.wip/distillation/findings/w3-lds-discoverability.md` per the BRIEF output contract.
Your detection contract feeds w5's `wip doctor`. Coordinate `wip.yaml` flags with w1.
