# Roadmap — {{slug}}

The plan of record for {{title}}. Brief: [`BRIEF.md`](./BRIEF.md). Locked
decisions graduate to `engineering/decisions/` (ADRs) as soon as they lock;
this roadmap holds the plan; each Step gets a
`workplans/step-NN-<slug>.md` when it starts.

Started: {{date}}.

---

<!--
Author the roadmap here: turn the BRIEF into Rounds and Steps. Until a real
step exists below, `wip next` reports "author the roadmap" — it will NOT point
you at `/wip:start`, because there is nothing to start yet. Uncomment and edit
the example, or write your own. Each round is one `## Round <N> — <title>` with
a one-sentence shipping criterion; each step is one
`- **step-NN — <title>** — <scope>` bullet (ids stay globally sequential).

## Round 1 — _title_

_One-sentence shipping criterion for the round._

- **step-01 — _title_** — _scope in a sentence._

Parallel lanes (ADR-0010): a round may run tracks in parallel. Put the shared
prereq in the main lane (a bare bullet), then one `### Lane <name>` subheading
per track. Lanes run in parallel; steps within a lane stay sequential. Step ids
stay globally sequential — the lane is a grouping, not a numbering namespace.

Issue-tracker addressing (ADR-0024): when `issue-tracker` is enabled, a step
bullet or a `## Round <N> — <title>` heading may carry a trailing
`[tracker: BDS-XX]` marker naming that node's tracker issue — `wip sync` then
surfaces and pushes its lifecycle forward. A `### Lane` heading takes NO tracker
key: a lane is a grouping, not a lifecycle node (ADR-0010), so it is never a
tracker node. The initiative-level anchor is NOT authored here — it is captured
at `/wip:intake` (`--anchor <ID>` or the brief `tracker-anchor:` front-matter
key) and persisted to `.wip.yaml` as the `initiative` node.

- **step-02 — shared prereq** — must land before either lane.

### Lane A
- **step-03 — track A work** — runs in parallel with Lane B.

### Lane B
- **step-04 — track B work** — runs in parallel with Lane A.
-->

---

## Deferred (decided-not-now)

_Items consciously postponed; keep the why so future-you can re-evaluate._

## Backlog (cross-cutting; see also `.wip/backlog.md`)

_Cross-cutting work that hasn't earned a round yet._
