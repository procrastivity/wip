# specs/ — Feature specs

The contracts `wip` is built against. A spec is the durable, build-ready statement of
*what* a feature does (the *why* is in `../decisions/`).

| Spec | Status |
|------|--------|
| `wip-plumbing-cli.md` | 🚧 planned — distillation roadmap step-05 |
